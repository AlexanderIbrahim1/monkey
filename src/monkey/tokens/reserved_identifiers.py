"""
This module contains constants that represent reserved identifiers. This allows us
to use a specific constant throughout the code, located at a single place. We don't
have to worry about whether the boolean is written as 'true', 'True', 'TRUE', etc.
"""

FUNCTION_IDENTIFIER = "fn"
LET_IDENTIFIER = "let"
TRUE_IDENTIFIER = "true"
FALSE_IDENTIFIER = "false"
RETURN_IDENTIFIER = "return"
IF_IDENTIFIER = "if"
ELSE_IDENTIFIER = "else"
